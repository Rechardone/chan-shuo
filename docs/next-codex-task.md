# 下一轮 Codex 开发任务

## 当前状态

已经建立 `codex/batch-01-bootstrap` 分支，并补齐第一批工程骨架。

## 继续开发目标

请在该分支继续完成：

1. 修复根目录 package scripts。
2. 补齐 DB DAO。
3. 将 agent CLI 接入 DB 写入。
4. 增加 JSON / CSV 导入。
5. 增加 OllamaProvider。
6. 桌面端升级为 Angular 18 + Tauri。

## 验收目标

- 能初始化 SQLite。
- 能导入 mock 数据。
- 能生成 mock AI 复盘。
- 能打开桌面端占位页面。
